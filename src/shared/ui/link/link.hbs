<div class="link link_{{type}} link_border {{#unless border}}link_no-border{{/unless}}">
    <a href="{{path}}" class="link__field">
        <p class="text-xsmall link__name">{{name}}</p>
    </a>
</div>
